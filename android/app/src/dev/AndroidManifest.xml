<?xml version="1.0" encoding="utf-8"?>
<!--
    dev flavor only: the app talks HTTP to a backend on the developer's
    machine (10.0.2.2 from the emulator). Cleartext is scoped to those hosts
    in res/xml/network_security_config.xml; the prod flavor keeps Android's
    HTTPS-only default.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
