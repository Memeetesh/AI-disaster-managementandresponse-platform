package com.drishti.citizen.core.ui.theme

import androidx.compose.material3.Typography

// Start from Material 3 defaults; swap in a custom font family + scale when
// the design pass happens.
val DrishtiTypography = Typography()
