# Standalone deps for the computer-vision prototypes. Deliberately separate
# from backend/requirements.txt — the FastAPI service does not import these.
#   python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
ultralytics>=8.3
opencv-python>=4.9
numpy>=1.26
requests>=2.31
